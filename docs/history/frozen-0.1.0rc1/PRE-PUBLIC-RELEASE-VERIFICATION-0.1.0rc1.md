# Pre-Public Release Verification — SolverPilot 0.1.0rc1

This checkpoint separates the post-migration release candidate from the historical `0.0.40rc2` artifact. It does not authorize publication.

Local verification must pass the full source regression, current API/backend snapshot checks, build/package checks, isolated-wheel tests, and source hygiene. External GitHub qualification remains mandatory for the exact release commit and exact artifacts before PyPI publication.

Historical Track P merge provenance remains frozen at `0.0.40rc2`; current package identity is `0.1.0rc1`.
