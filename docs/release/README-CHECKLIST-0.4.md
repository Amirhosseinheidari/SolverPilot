# SolverPilot 0.4 documentation contract

- Current version, examples and optional extras match package metadata.
- Global QP/MIQP and bounded factorable MINLP are explicit separate paths.
- Independent conic/PSD evidence states its arithmetic and scope limits.
- Learned production routing remains disabled; shadow observations are separate.
- CPU PDLP is isolated; GPU and exact SCIP readiness are not execution support.
- LP witness recovery is optional, bounded and independently rechecked.
- Reuse observations do not imply factorization reuse.
- Historical snapshots remain intact; current snapshots record additive options.
- Publication uses the exact wheel from successful release qualification.
